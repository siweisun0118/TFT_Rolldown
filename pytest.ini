[pytest]
# The new unit-test suite lives in tests/. The legacy `testing/` directory
# contains throw-away scripts (some run a Qt app or call input() at import
# time) and must NOT be collected.
testpaths = tests
norecursedirs = testing .git .pytest_cache __pycache__ General?Assets TFT_Set_*
